{
  "name": "smart-1-sites-creator",
  "version": "2.2.0",
  "private": true,
  "type": "module",
  "scripts": {
    "start": "node server.js",
    "check": "node --check server.js && node --check crawler.js && node --check ai-planner.js && node --check simvoly-client.js && node --check template-scorer.js && node --check public/app.js"
  },
  "dependencies": {
    "cheerio": "^1.0.0",
    "dotenv": "^16.4.5",
    "express": "^4.21.2",
    "openai": "^5.0.0"
  },
  "engines": {
    "node": ">=20"
  }
}
