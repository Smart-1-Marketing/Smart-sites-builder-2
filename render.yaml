services:
  - type: web
    name: smart-1-sites-creator
    runtime: node
    plan: starter
    buildCommand: npm install
    startCommand: npm start
    healthCheckPath: /health
    envVars:
      - fromGroup: "simvy keys"
